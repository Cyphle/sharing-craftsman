version: '3'

services:
  library:
    build: .
    container_name: sc_library
    ports:
      - <APP_PORT>
    volumes:
     - <UPLOAD_PATH>:/app/upload
     - <LOGS_PATH>:/app/logs

networks:
  default:
    external:
      name: <DOCKER_NETWORK>